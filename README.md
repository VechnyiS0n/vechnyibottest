# vechnyibottest

🎓 **Анонимная система оценки школьных уроков**

Веб-сервис и Telegram-бот для сбора **анонимной обратной связи** от учащихся и анализа качества уроков учителем.

Проект разработан в рамках **индивидуального проекта** для школы и направлен на повышение качества образовательного процесса за счёт честной, безопасной и наглядной обратной связи.

---

## 📌 Идея проекта

Во время урока или после него учащиеся могут:

- анонимно оценить, насколько понятен был материал;
- поставить оценку по 5-балльной шкале;
- оставить комментарий;
- задать вопрос учителю без указания имени.

Учитель, в свою очередь, получает:

- сводную статистику по каждому уроку;
- визуальное отображение настроения класса;
- список анонимных вопросов и отзывов;
- индикатор проблемных уроков.

👉 **Никакие персональные данные учащихся не собираются и не хранятся.**

---

## 🧩 Функциональные возможности

### 🤖 Telegram-бот (для учащихся)

- Выбор урока по коду
- Оценка понимания материала:
  - 👍 Понятно  
  - 😐 Средне  
  - 👎 Непонятно
- Оценка по 5-балльной шкале
- Анонимные комментарии
- Анонимные вопросы учителю
- Пошаговый диалог (FSM)
- Отсутствие привязки к личности пользователя

---

### 🌐 Веб-панель учителя

- Создание уроков и автоматическая генерация кодов
- Общая статистика по всем урокам:
  - количество уроков;
  - общее число оценок;
  - средняя оценка
- Статус уроков:
  - 🟢 **Всё хорошо**
  - 🔴 **Есть проблемы**
- Страница отдельного урока:
  - общая статистика;
  - настроение класса (круговая диаграмма);
  - распределение оценок (1–5);
  - список анонимных вопросов;
  - список отзывов с визуализацией mood (👍 😐 👎)
- Минималистичный тёмный интерфейс
- Вся аналитика без использования JavaScript

---

## 🛠 Используемые технологии

### Backend
- **Python 3**
- **Flask** — веб-фреймворк
- **aiogram v3** — Telegram-бот
- **PostgreSQL** — база данных
- **psycopg2** — работа с БД
- **python-dotenv** — переменные окружения

### Frontend
- **HTML + Jinja2**
- **CSS**
- Визуальные карточки и графики (без JS)

---

## 📂 Структура проекта

```text
feedback_system/
├── bot/
│   ├── bot.py          # Логика Telegram-бота
│   └── states.py       # Состояния (FSM)
│
├── web/
│   ├── app.py          # Flask-приложение
│   ├── templates/
│   │   ├── index.html  # Панель учителя
│   │   └── lesson.html # Страница урока
│   └── static/
│       └── style.css   # Стили оформления
│
├── db.py               # Работа с PostgreSQL
├── models.sql          # Структура базы данных
├── requirements.txt    # Зависимости
├── .env.example        # Пример переменных окружения
└── README.md

```

---


## ⚙️ Установка и запуск

### 1️⃣ Установка зависимостей

Рекомендуется использовать виртуальное окружение (venv), чтобы зависимости проекта не конфликтовали с системными пакетами.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
````

---

### 2️⃣ Настройка переменных окружения

Создайте файл `.env` в корне проекта (рядом с `requirements.txt`). Пример:

```env
BOT_TOKEN=your_telegram_bot_token_here

DB_HOST=localhost
DB_PORT=5432
DB_NAME=feedback_db
DB_USER=feedback_user
DB_PASSWORD=feedback_pass

FLASK_SECRET_KEY=supersecret
```

**Пояснение:**

* `BOT_TOKEN` — токен Telegram-бота
* `DB_*` — параметры подключения к PostgreSQL
* `FLASK_SECRET_KEY` — секретный ключ Flask (нужен для работы формы/безопасности)

---

### 3️⃣ Создание базы данных

1. Создайте базу и пользователя (пример для PostgreSQL):

```bash
psql -U postgres
```

```sql
CREATE DATABASE feedback_db;
CREATE USER feedback_user WITH PASSWORD 'feedback_pass';
GRANT ALL PRIVILEGES ON DATABASE feedback_db TO feedback_user;
```

2. Примените структуру таблиц:

```bash
psql -U feedback_user -d feedback_db -h localhost -f models.sql
```

---

### 4️⃣ Запуск веб-сервиса (Flask)

```bash
python web/app.py
```

Откройте в браузере:

```
http://127.0.0.1:5000/
```

---

### 5️⃣ Запуск Telegram-бота (aiogram)

В отдельном терминале:

```bash
python bot/bot.py
```

После запуска бот станет доступен в Telegram.

---

## 🔒 Анонимность и безопасность

* Telegram-бот **не хранит персональные данные** учащихся (имена/никнеймы).
* Оценки и вопросы сохраняются в базе данных **без привязки к личности**.
* Учитель получает доступ только к **обобщённой статистике**, а также к текстам отзывов/вопросов в обезличенном виде.
* Такой подход помогает учащимся оставлять **честную обратную связь** без страха, что их узнают.

---

## 🎓 Образовательная ценность проекта

В рамках проекта были изучены и применены:

* основы клиент-серверной архитектуры (бот + сервер + база данных);
* работа с реляционной базой данных PostgreSQL (таблицы, связи, запросы);
* разработка Telegram-ботов на Python с использованием aiogram (состояния, кнопки, сценарии диалогов);
* разработка веб-приложений на Flask (роутинг, шаблоны Jinja2, работа с формами);
* принципы проектирования системы анонимной обратной связи.

Проект демонстрирует практическое применение информационных технологий для решения реальной задачи в школьном образовании.

---

## 🚀 Возможные направления развития

* Добавление авторизации для учителей (личный кабинет).
* Поддержка нескольких классов/предметов и фильтрация статистики.
* Экспорт результатов в PDF/Excel.
* Расширение аналитики (динамика по времени, сравнение уроков, дополнительные графики).
* Улучшение интерфейса (адаптация под мобильные устройства, улучшенная визуализация данных).



